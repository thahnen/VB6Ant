# Executable created by the VB6.exe IDE directly
*.exe

# Worspace file that changes everytime the user works with the IDE
*.vbw

# Build directory containing all the outputs and logs
build/

# SonarQube Cloud Scanner (hidden file)
.sonar-project.properties
